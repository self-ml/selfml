#!/usr/bin/env ruby

require 'selfml'
require 'json'

input = ARGF.read
$stdout << SelfML.parse(input, SelfML::Engines::Hash)
$stdout << "\n"
$stdout << SelfML.parse(input, SelfML::Engines::Array)

# $stdout << output.to_json
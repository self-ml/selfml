#!/usr/bin/env ruby

require 'selfml'
require 'json'

input = ARGF.read
$stdout << SelfML.parse(input, SelfML::Engines::HashTransformer)
$stdout << "\n"
$stdout << SelfML.parse(input, SelfML::Engines::ArrayTransformer)

# $stdout << output.to_json